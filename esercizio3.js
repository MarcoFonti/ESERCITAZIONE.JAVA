//stampato sulla console: ((2 + 4) * (5 + 2) - 2) / 5

const sum = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
const log = (value) => console.log(value);


const result = ((2 + 4) * (5 + 2) - 2) / 5;

log(result); // Stampo il risultato sulla console

